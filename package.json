{
  "name": "json-server-demo",
  "version": "1.0.0",
  "description": "JSON Server API",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
